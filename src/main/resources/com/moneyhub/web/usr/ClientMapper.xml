<?xml version="1.0" encoding="UTF-8"?><!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.moneyhub.web.usr.ClientMapper">
    
	<select id="selectByIdPw" resultType="Client">
       SELECT CID, PWD, CNAME FROM CLIENT WHERE CID LIKE #{cid} AND PWD LIKE #{pwd}
    </select>
    
    <insert id="insertClient" parameterType="Client">
         INSERT INTO CLIENT (CID, PWD, CNAME) VALUES 
         (#{cid}, #{pwd}, #{cname})
    </insert>
    
    <select id="existId" resultType="int">
    	SELECT count(*) FROM CLIENT WHERE CID LIKE #{cid}
    </select>
    
</mapper>